#include "RE/B/BSTempEffectWeaponBlood.h"

namespace RE
{
	void BSTempEffectWeaponBlood::ClearEffectForWeapon(NiAVObject* a_weapon3D)
	{
		using func_t = decltype(&BSTempEffectWeaponBlood::ClearEffectForWeapon);
		REL::Relocation<func_t> func{ REL::ID(30154) };
		return func(a_weapon3D);
	}
}
